spring:
  application:
    name: ficr-iaedu-backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:mock}

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
